#include "main.h"
/**
 *print_alphabet_x10: function to print alphabet 10 times
 */
void print_alphabet_x10(void)
{
	char f, q;
		for (f = 0; f <= 9; f++);
	{
		for (q = 'a'; q <= 'z'; q++)
		{
			_putchar(q);
			_putchar('\n');
		}
	}
}


	
